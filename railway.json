{
  "name": "FixMyRV Backend API",
  "build": {
    "command": "cd backend && npm install && npm run build"
  },
  "start": {
    "command": "cd backend && npm start"
  },
  "environment": {
    "NODE_ENV": "production"
  },
  "deploy": {
    "detectEngine": false,
    "buildCommand": "cd backend && npm install && npm run build",
    "startCommand": "cd backend && npm start"
  }
}
